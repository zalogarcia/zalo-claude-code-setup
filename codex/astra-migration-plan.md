# Astra setup migration

Scope: edit only the source workspace. Git is read only. External settings and
bridge changes are proposals. Never bypass the sandbox to regenerate files.

1. Read the supplied guide and source setup. Done.
2. Apply adapter behavior, preserve gates, migrate fable to Astra xhigh,
   correct wrapper documentation, and update design notes. Done.
3. Audit the generated manual and all 58 skills; review external bridge and
   configuration surfaces and prepare exact proposals. Done: 58 skills plus
   generated AGENTS scanned; 93 records and four external diffs delivered.
4. Run required Python suites, source QA, sync all, sync check, generated-file
   checks, and zero Unicode dash checks. Done with activation blocked: suites
   passed, independent QA confirmed fixes, manual updated, four agent files
   remain stale because the sandbox denies generated-directory writes.
5. Deliver findings, proposals, evidence, and remaining questions. Done in
   the linked audit, proposal, and verification reports and final response.

Acceptance: guide prompts adapted, opus high preserved, wrapper parameters
reviewed, full audit inventory measured, external files untouched by manual
edits, test results recorded, projection status reported honestly, and every
authored file contains zero em dashes and zero en dashes.
